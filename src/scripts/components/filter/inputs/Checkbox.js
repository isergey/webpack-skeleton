'use strict';
import React from 'react';
import marked from 'marked';
export default React.createClass({
  render() {
    return (
      <label><input type="checkbox" name="browser" value="1"/>1</label>
    );
  }
});